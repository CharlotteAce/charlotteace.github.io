---
// src/components/QrCode.astro
import QRCode from 'qrcode';

interface Props {
  url: string;
}

const { url } = Astro.props;
const qrDataUrl = await QRCode.toDataURL(url);
---

<img class="qrcode" src={qrDataUrl} alt={`QR code for ${url}`} style="width: 80px; height: 80px;" />