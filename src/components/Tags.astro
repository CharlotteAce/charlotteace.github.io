---
const { tags } = Astro.props;
console.log(tags)
---
<div class="tags">
    {tags.map((tag:string[]) => (<a class="tag" href={`https://charlotteace.github.io/contents/tags/${tag}`}>{tag}</a>))}
</div>