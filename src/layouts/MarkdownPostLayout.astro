---
import BaseLayout from './BaseLayout.astro';
const { frontmatter } = Astro.props;
---
<BaseLayout pageTitle={frontmatter.title} pageUrl={frontmatter.pageUrl} releaseDate={frontmatter.releaseDate} updateDate={frontmatter.updateDate}>
    <slot />
</BaseLayout>